<%= error_summary(title: "Are you sure you want to delete #{@organisation.name}") do %>
  <%= govuk_button_to("Yes, remove this organisation",
      super_admin_organisation_path(@organisation),
      method: :delete,
      warning: true) %>
<% end %>
